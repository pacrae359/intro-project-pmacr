/* Just commenting this out for now while I work on getting the SQLLite database working!
var utils = require("chai")
var expect = chai.expect;

describe("Add Person", () => {

    beforeEach(() => {
        // Setting up some person information to
        var name = "TestName"
        var email = "TestEmail@Email.com"
        var notes = "Additional notes"
    });

    it("should add a person with valid input", async () => {
        
    })
});

describe("Edit Person", () => {
    it("should edit a person with valid input", async () => {

    })
});

describe("Add Person", () => {
    it("should add a person with valid input", async () => {

    })
});
*/